# AgriStress — development & practical test stack (pure wheels).
# Installs core runtime first, then the test/ML/serving deps the pytest suite
# uses plus dev tooling. Mirrors the [project.optional-dependencies] `test` +
# `dev` extras in pyproject.toml. Equivalent to: pip install -e ".[dev]".
-r requirements.txt

# --- machine learning (sklearn stack; xgboost/lightgbm optional, auto-fallback) ---
scikit-learn>=1.4
scikit-image>=0.22
joblib>=1.3

# --- serving / CLI ---
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pillow>=10.0

# --- datacube I/O used by tests ---
pyarrow>=15.0
zarr>=2.17

# --- dev tooling ---
pytest>=8.1
pytest-cov>=5.0
ruff>=0.4
mypy>=1.9
black>=24.3
pre-commit>=3.7
types-PyYAML>=6.0
