# AgriStress — core runtime (pinned-ish). Mirrors [project.dependencies] in
# pyproject.toml. For ML/serving/viz extras install via:
#   pip install -e ".[all]"
# NOTE: the geospatial native stack (GDAL-backed: rasterio, geopandas, pyproj)
# is far easier to install via conda (see environment.yml). Use this file on
# systems where binary wheels are available (Linux/macOS x86_64, py3.11+).

# --- foundation ---
numpy~=1.26
pandas~=2.2
pydantic~=2.7
pydantic-settings~=2.3
PyYAML~=6.0
typer~=0.12
rich~=13.7
python-dotenv~=1.0

# --- geospatial core / datacube ---
earthengine-api~=0.1.400
geemap~=0.32
rasterio~=1.3
rioxarray~=0.15
xarray~=2024.6
dask[array]~=2024.6
zarr~=2.18
geopandas~=0.14
shapely~=2.0
pyproj~=3.6
h3~=4.0
pystac-client~=0.8
stackstac~=0.5
odc-stac~=0.3
